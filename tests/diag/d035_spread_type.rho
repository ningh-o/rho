// `xs...` must be exactly the parameter's slice type
fn f(rest: i64...) -> i32 {
  return 0;
}

fn main() -> i32 {
  let words: []string = make([]string, 1);
  words[0] = "no";
  return f(words...);
}
