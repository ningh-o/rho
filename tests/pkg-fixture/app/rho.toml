# fixture application manifest — pinned and verified by run.sh.
# The strlib rev must equal the second commit of the deterministic remote
# that run.sh builds (fixed dates, fixed identities).
[package]
name = "fixture-app"
version = "0.1.0"

[dependencies.mathx]
path = "vendor/mathx"

[dependencies.strlib]
git = "https://rho-fixture.local/strlib"
rev = "0d744dc6dc4f4d0da72fe7e1c63cfd027e914939"
