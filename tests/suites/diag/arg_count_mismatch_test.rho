// expect: no overload of add matches the arguments
// spec: type-system.md §5
// overload resolution is exact-match-unique: a call with the wrong
// argument count matches no overload
fn add(a: i32, b: i32) -> i32 {
  return (a + b);
}
fn main() -> i32 {
  add(1);
  return 0;
}
