// expect: named arguments are only valid in constructors
// spec: check — calls
// plain calls are positional; 'name: value' arguments are reserved
// for struct/variant constructors
fn f(a: i32) -> i32 { return a; }

fn main() -> i32 {
  let x = f(a: 1);
  return 0;
}
