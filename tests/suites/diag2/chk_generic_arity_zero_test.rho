// expect: struct P takes 0 generic argument(s), got 1
// spec: check — generics
// a non-generic struct accepts no type arguments
struct P { x: i32 }

fn main() -> i32 {
  let p: P[i32] = new P { x: 1 };
  return 0;
}
