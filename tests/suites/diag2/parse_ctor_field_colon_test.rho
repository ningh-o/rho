// expect: after field name
// spec: syntax.md — constructors
// constructor fields are written 'name: value' — the colon is
// required
struct S { x: i32 }

fn main() -> i32 {
  let s = new S { x 1 };
  return 0;
}
