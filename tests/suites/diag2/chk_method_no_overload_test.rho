// expect: no overload of method 'get' matches the arguments
// spec: check — methods
// a method call whose argument shapes match no overload of the
// method is refused
struct S { x: i32 }

fn S.get(self) -> i32 { return self.x; }

fn main() -> i32 {
  let s = new S { x: 1 };
  let a = s.get(1);
  return 0;
}
