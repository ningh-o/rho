// expect: calling a non-function value
// spec: check — calls
// an i32 expression cannot be called: only fn values and closures
// are callable
fn main() -> i32 {
  let x = 5;
  let g = (x + 1)(2);
  return 0;
}
