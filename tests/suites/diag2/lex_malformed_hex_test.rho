// expect: malformed hex literal
// spec: syntax.md — lexer
// 0x must be followed by at least one hex digit; an identifier
// character glued to the prefix poisons the literal
fn main() -> i32 {
  let x = 0xGG;
  return 0;
}
