// expect: format string has 2 {} hole(s) but 1 value(s) given
// spec: check — printf family
// every {} hole in a literal format string pairs with one value
fn main() -> i32 {
  printf("{} {}\n", 1);
  return 0;
}
