// exit: 0
fn main() -> i32 {
  // content may begin on the opener's own line; no newline is required
  // and none is inserted
  let s: string = """ x
  """;
  printf("[{}]\n", s);
  printf("{}\n", len(s) as i32);
  return 0;
}
