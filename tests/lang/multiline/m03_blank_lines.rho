// exit: 0
fn main() -> i32 {
  // blank lines are content: an empty line stays an empty line
  let s: string = """

  first

  last

  """;
  printf("[{}]\n", s);
  // a whitespace-only line keeps its spaces
  let w: string = """
  top
 
  bottom
  """;
  printf("[{}]\n", w);
  return 0;
}
