// expect: takes 1 generic argument(s), got 2
// expect: cannot infer the generic arguments
struct Box[T] {
  v: T,
}

fn main() -> i32 {
  let a: *Box[i32, i64] = new Box[i32, i64] { v: 1 };
  let b = new Box { v: 2 };
  return 0;
}
