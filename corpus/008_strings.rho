// exit: 6
fn word_len(s: string) -> i32 {
  return len(s) as i32;
}

fn main() -> i32 {
  let s: string = "rho language";
  let head: string = s[0..3];
  let mut n: i32 = 0;
  if head == "rho" {
    n += 1;
  }
  if s[3] == " "[0] {
    n += 2;
  }
  n += word_len(head);
  return n;
}
